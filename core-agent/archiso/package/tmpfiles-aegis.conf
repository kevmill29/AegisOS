# systemd-sysusers records the aegis user's home but does not create it; make
# it here (runs after sysusers at boot) so cage/Electron/Steam have a writable
# HOME. Steam in particular needs a real home directory.
d /home/aegis 0700 aegis aegis -
